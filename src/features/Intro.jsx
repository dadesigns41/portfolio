import SectionHeader from '../components/SectionHeader';
import CTAButton from '../components/CTAButton';

function Intro() {
  return (
    <section id="intro" className="intro">
      <h1>Hi, I’m Danny</h1>

      <p>
        I’m a frontend developer in progress, focused on learning modern
        JavaScript and React by building real projects.
      </p>

      <p>
        This portfolio documents my journey — the tools I use, the projects I
        build, and how I approach problem solving.
      </p>
    </section>
  );
}

export default Intro;
